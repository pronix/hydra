<ul class="subMenu">
  <li>
    <%= link_to I18n.t("my_settings"), settings_path, :class => ('current' if params[:controller]["settings"]) %>
  </li>
  <li>
    <%= link_to(I18n.t("users"), users_path, :class => ('current' if params[:controller]["users"])) if current_user.admin? %>
  </li>
  <li>
    <%= link_to I18n.t("proxy"), proxies_path, :class => ('current' if params[:controller]["proxies"] ) %>
  </li>
</ul>

