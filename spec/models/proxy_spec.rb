require 'spec_helper'

describe Proxy do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Proxy.create!(@valid_attributes)
  end
end
