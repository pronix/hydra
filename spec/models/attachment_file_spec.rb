require 'spec_helper'

describe AttachmentFile do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    AttachmentFile.create!(@valid_attributes)
  end
end
