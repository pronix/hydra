require 'spec_helper'

describe Macros do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Macros.create!(@valid_attributes)
  end
end
